var supertest = require('supertest');
var should = require('should');

describe('Routing',function() {
		it('should pass', function(done) {
			done();
		});

		it('should pass again', function(done) {
			done();
		})
})